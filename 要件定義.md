要件定義書: Slack 通知フォーマット刷新 & 翻訳要約精度改善
プロジェクト概要

海外 AI ニュースを自動取得し、翻訳・要約して Slack に通知する既存パイプラインを改修する。
従来は「500 字自然文要約」を前提としていたが、Slack 上で視認性が悪いため、以下の改善を行う。

改修目的

Slack 通知の可読性向上

タイトル（日本語見出し調）

カテゴリ/スコア（AI ニュースの重要度）

要点（3〜5 点の箇条書き）

「📖 記事を読む」ボタン

Divider で記事間を区切る

翻訳・要約精度の改善

要約は「日本語箇条書きリスト」を GPT に直接生成させる

タイトル翻訳は「日本語の新聞見出し風（40 字以内）」に整える

対象ファイルと改修内容

1. summarize.py

現状: 500 字の自然文要約を返す

改修後:

summarize(text: str) -> list[str]

GPT に「日本語の要点 3〜5 点の箇条書き」を直接生成させる

各要点は 120 文字以内

Fallback 時は正規表現で文分割し箇条書き化

2. translate.py

現状: Google Translate ＋正規表現で見出し調を擬似処理

改修後:

translate_headline(title_en: str) -> str

GPT に「40 字以内の日本語見出し調」を直接生成させる

文末は「〜を発表」「〜開始」「〜公開」など名詞止めを基本とする

Fallback 時のみ Google Translate を使用

3. notify_slack.py

現状: summary: str を Slack に送信

改修後:

引数を points: list[str] に変更

Slack Block Kit を使用し以下のレイアウトで送信

📰 {タイトル}
カテゴリ: {category} ・ スコア: {score}/10
_要点_
・要点 1
・要点 2
・要点 3
[📖 記事を読む]
--- divider

4. main.py

現状: summary を受け取り Slack 送信

改修後:

points = summarize(article_body) を呼び出し

send_to_slack(channel, title, url, points, category, score) を渡す

5. save_notion.py

現状: 500 字サマリを保存

改修後:

points を "\n".join(points) にまとめて保存

（オプション）Notion には「500 字サマリ」と「要点リスト」の両方保存可能に

6. send_batch.py / send_one.py

現状: summary を Slack に送信

改修後: points に対応するよう修正

非機能要件

OpenAI API は gpt-4o-mini を使用（翻訳・要約ともに）

Fallback 時は Google Translate / 簡易要約を利用

Slack 通知は unfurl_links=False, unfurl_media=False を維持してプレビュー展開を抑止

完了の定義

Slack に送信される記事通知が「タイトル・カテゴリ/スコア・要点（箇条書き）・記事リンク」の形式で表示されること

要点が最大 5 点に収まり、1 点あたり 120 字以内になっていること

日本語タイトルが 40 字以内の自然な新聞見出し調になっていること

Notion 保存時に要点が崩れず保存されていること
